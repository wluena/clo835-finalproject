apiVersion: v1
kind: ServiceAccount
metadata:
  name: clo835
  namespace: final
  annotations:
    # Replace <ACCOUNT_ID> with your AWS Account ID
    # Replace <IAM_ROLE_NAME> with the role you created in IAM for S3 access
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/<IAM_ROLE_NAME>